/**
 * NEXORA PULSECARE - SUPABASE REMOVED
 * Database & Auth are powered strictly by Express + MongoDB Atlas (database 'prplus').
 */

import { authService } from "./services/authService.js";

export const supabase = null;
export default authService;
